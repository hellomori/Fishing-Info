.search-result
  - @posts.each do |post|
    .search-result__once
      = link_to image_tag(post.image.url, size: "320x280", alt: "画像が見つかりません"), post_path(post)
      .search-result__once__date
        = post.created_at.to_s(:datetime_jp)
  = paginate @posts